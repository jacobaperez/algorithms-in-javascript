const ds = require('../tree');
const chai = require('chai');
const expect = chai.expect;

// let tree = ds.tree();

describe("Tree", () => {

  it('should create a new tree', () => {
    expect(0).to.eql(1);
  })


  it('should have no children', () => {
    expect(2).to.eql(1);
  })


})
